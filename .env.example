# VanishVoice Environment Variables
# Copy this file to .env.local (or .env) and fill in your values

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Push Notifications (Optional)
# Required only if you're using Firebase Cloud Messaging for Android push notifications
# The google-services.json file is referenced in app.json but not tracked in git
# Download it from your Firebase project console

# Development Build Configuration
# These are automatically handled by EAS Build but you might need them for local development
# EXPO_PUBLIC_APP_VERSION=1.0.0

# Notes:
# - EXPO_PUBLIC_ prefix is required for environment variables to be accessible in React Native
# - Never commit your actual .env file with real credentials
# - Add .env* to your .gitignore (except .env.example)
# - For production builds, set these in your EAS Build secrets or CI/CD environment
