# Automatically use Node.js version from .node-version file
use node

# Optional: Add project-specific environment variables here
# export NODE_ENV=development
# export EXPO_USE_LOCAL_CLI=1

# Print Node version when entering directory
echo "🚀 VanishVoice: Using Node.js $(node --version)"